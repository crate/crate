apply plugin: 'java-library'
apply from: "$rootDir/gradle/javaModule.gradle"
archivesBaseName = 'crate-cli'

dependencies {
    implementation project(':libs:shared')
    api "net.sf.jopt-simple:jopt-simple:${versions.jopt_simple}"
}
