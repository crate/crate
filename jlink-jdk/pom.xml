<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.crate</groupId>
    <artifactId>crate</artifactId>
    <version>6.2.0</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  <artifactId>jlink-jdk</artifactId>
  <packaging>jlink</packaging>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-jlink-plugin</artifactId>
        <version>${versions.plugin.jlink}</version>
        <extensions>true</extensions>
        <configuration>
          <!--
            The `jlink` command supports cross-platform builds by providing a
            module-path to jmods files for the desired platform.

            The files are downloaded with the `jmods` profile
          -->
          <modulePaths>
            <modulePath>${project.build.directory}/jdk-${versions.jdkbuild}-jmods</modulePath>
          </modulePaths>

          <addModules>
            <addModule>java.base</addModule>
            <addModule>java.logging</addModule>
            <addModule>java.compiler</addModule>
            <addModule>java.desktop</addModule>
            <addModule>java.management</addModule>
            <addModule>java.naming</addModule>
            <addModule>java.rmi</addModule>
            <addModule>java.scripting</addModule>
            <addModule>java.security.jgss</addModule>
            <addModule>java.security.sasl</addModule>
            <addModule>java.sql</addModule>
            <addModule>jdk.unsupported</addModule>
            <addModule>jdk.management</addModule>
            <addModule>jdk.management.agent</addModule>
            <addModule>jdk.crypto.ec</addModule>
            <addModule>jdk.incubator.vector</addModule>

            <!-- These are required to use https://github.com/crate/jmx_exporter -->
            <addModule>java.instrument</addModule>
            <addModule>jdk.httpserver</addModule>

            <!-- To support -XX:StartFlightRecording=[...] -->
            <addModule>jdk.jfr</addModule>

            <addModule>jdk.jcmd</addModule>
            <addModule>jdk.jdwp.agent</addModule>
          </addModules>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>jmods</id>
      <build>
        <plugins>
          <plugin>
            <groupId>com.googlecode.maven-download-plugin</groupId>
            <artifactId>download-maven-plugin</artifactId>
            <version>${versions.plugin.download}</version>
            <executions>
              <execution>
                <id>download-jmods</id>
                <phase>prepare-package</phase>
                <goals>
                  <goal>wget</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <url>https://api.adoptium.net/v3/binary/version/jdk-${versions.jdkbuild}/${jmods.os}/${jmods.arch}/jmods/hotspot/normal/eclipse?project=jdk</url>
              <unpack>true</unpack>
              <outputFileName>jmods.tar.gz</outputFileName>
              <outputDirectory>${project.build.directory}</outputDirectory>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
